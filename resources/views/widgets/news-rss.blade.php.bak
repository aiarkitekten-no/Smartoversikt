<div class="p-2">
    @if(isset($data['error']))
        <div class="text-center text-gray-500">
            <p class="text-xs">{{ $data['error'] }}</p>
        </div>
    @else
        @if(isset($data['items']) && count($data['items']) > 0)
            <div class="space-y-1 max-h-80 overflow-y-auto">
                @foreach($data['items'] as $item)
                    <a href="{{ $item['link'] }}" target="_blank" 
                       class="block p-1.5 hover:bg-blue-50 rounded transition-colors group">
                        <div class="flex items-start gap-1.5">
                            <span class="text-blue-600 text-xs mt-0.5">📰</span>
                            <div class="flex-1 min-w-0">
                                <h4 class="text-xs font-semibold text-gray-900 group-hover:text-blue-700 leading-tight line-clamp-2">
                                    {{ $item['title'] }}
                                </h4>
                                <div class="flex items-center gap-1.5 text-xs text-gray-500 mt-0.5">
                                    <span class="font-medium">{{ $item['source'] }}</span>
                                    <span>•</span>
                                    <span>{{ \Carbon\Carbon::parse($item['date'])->diffForHumans() }}</span>
                                </div>
                            </div>
                        </div>
                    </a>
                @endforeach
            </div>
            
            <div class="mt-0.5 pt-2 border-t border-gray-200 text-center">
                <p class="text-xs text-gray-500">
                    {{ count($data['items']) }} artikler fra {{ $data['total_feeds'] }} kilder
                </p>
            </div>
        @else
            <div class="text-center py-1 text-gray-500">
                <p class="text-xs">Ingen nyheter tilgjengelig</p>
            </div>
        @endif
    @endif
</div>
